# backend/database.py

def init_db() -> None:
    """
    Dummy database initialization.

    Later this will be replaced with real DB setup
    (engine, session, migrations, etc.).
    For now it just prints a message so we know it ran.
    """
    print("✅ init_db() called – dummy database initialized.")
